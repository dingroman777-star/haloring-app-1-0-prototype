(() => {
  window.createHaloRhythmManagementPage = ({ esc }) => {
    const icon = name => `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="${{calendar:'M5 5h14v15H5zM8 3v4m8-4v4M5 10h14m-10 4h1m4 0h1m-6 3h1',pause:'M8 5v14M16 5v14',play:'m8 5 11 7-11 7z',record:'M5 4h14v16H5zM8 9h8m-8 4h8m-8 4h4',trash:'M4 7h16M9 4h6v3M6 7l1 13h10l1-13M10 11v5m4-5v5',check:'m5 12 4 4L19 6'}[name] || ''}"/></svg>`;
    const button = (text, action, style = '', disabled = false) => `<button type="button" class="${style}" data-action="rh-manage:${action}"${disabled ? ' disabled' : ''}>${text}</button>`;
    function body(view, feedback = '') {
      const header = '<header class="rh-manage-header"><button type="button" data-action="previous">← 返回</button><h1>管理节律记录</h1></header>';
      if (!view.canManage) return `<article class="rhythm-management-page">${header}<section class="rh-manage-blocked"><h2>暂时无法管理记录</h2><p role="alert">${esc(view.error || '请重新登录后再查看。')}</p><button class="secondary" data-action="go:SET-01">查看数据与隐私</button></section></article>`;
      const cycle = view.mode === 'cycle', dirty = view.settingsDirty || view.conflict;
      const pending = { conflict: '待核对', insufficient: '待补充', error: '暂不可用' }[view.status];
      const status = view.deleted ? '尚未重新设置' : pending || (view.paused && cycle ? '已暂停' : cycle && view.confirmed ? '已开启' : '未启用');
      const note = pending ? '周期日期暂不展示，感受记录仍可回看。可以前往设置核对。' : view.paused && cycle ? '周期日期暂不展示，感受仍可记录和回看。' : cycle && view.confirmed ? '暂停只隐藏周期日期，不会删除感受或已保存的日期。' : '目前只记录感受，想加入周期日期时再设置。';
      return `<article class="rhythm-management-page">${header}${view.deleted ? `<section class="rh-manage-cleared">${icon('check')}<div><strong>节律数据已清空</strong><p>可以重新开始记录，旧内容不会恢复。</p></div></section>` : ''}<section class="rh-manage-journal"><h2>按天管理</h2>${button(`${icon('calendar')}<span><strong>${view.recordCount ? '查看和修改记录' : '打开感受日历'}</strong><small>${view.recordCount ? `已保存 ${view.recordCount} 天感受 · 从日历选一天` : '还没有保存的感受，随时可以开始记录'}</small></span><i aria-hidden="true">›</i>`, 'calendar', 'rh-manage-row')}</section><section class="rh-manage-cycle"><div class="rh-manage-cycle-heading"><h2>周期展示</h2><span>${esc(status)}</span></div><p>${note}</p>${view.canPause || view.canResume || cycle && view.hasSettings ? `<div class="rh-manage-visibility">${button(`${icon(view.paused ? 'play' : 'pause')}${view.paused ? '恢复周期展示' : '暂停周期展示'}`, view.paused ? 'resume' : 'pause', 'secondary', view.paused ? !view.canResume : !view.canPause)}</div>` : ''}${dirty ? '<p class="rh-manage-draft">设置里还有未保存或需要核对的修改，请先处理后再调整展示。</p>' : ''}${button(`${icon('record')}<span>${dirty ? '回到设置，继续修改' : '记录方式与周期设置'}</span><i aria-hidden="true">›</i>`, 'settings', 'rh-manage-settings')}</section><p class="rh-manage-feedback" role="status">${esc(feedback || view.error || '')}</p><section class="rh-manage-danger"><h2>删除全部节律数据</h2><p>包括实际经期、周期设置、感受记录和未保存的草稿。</p>${button(`${icon('trash')}删除全部节律数据`, 'delete', 'rh-manage-delete', !view.canDelete)}<small>${view.canDelete ? '下一步先核对删除范围，不会立即删除。' : '当前没有可删除的节律数据。'}</small></section><p class="rh-manage-boundary">戒指数据、其他日常记录和已发送的聊天内容不受影响。</p></article>`;
    }
    function confirmation(token, summary) {
      return `<div class="modal-backdrop"><section class="modal info-modal rh-manage-confirm" data-token="${esc(token)}" role="dialog" aria-modal="true" aria-labelledby="rh-manage-confirm-title"><h2 id="rh-manage-confirm-title">删除全部节律数据？</h2><p>只删除当前账号的节律数据：</p><ul><li>已保存的感受记录${Number.isInteger(summary?.recordCount) ? `（${summary.recordCount} 天）` : ''}</li><li>实际经期开始/结束、周期设置和未保存的草稿</li><li>同时关闭感受提醒、经期预测与临近提醒</li></ul><p class="rh-manage-warning">删除后无法恢复。已发送的聊天原文、戒指数据和其他日常记录会保留。</p><p class="rh-manage-modal-error" role="alert"></p><div class="rh-manage-confirm-actions">${button('暂不删除', 'cancel', 'secondary')}${button('确认删除节律数据', 'delete-confirm', 'rh-manage-delete')}</div></section></div>`;
    }
    return { body, confirmation };
  };
})();
